{
  "name": "petscans-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers API proxy for PetScans. Holds third-party API keys server-side, caches lookups, rate-limits per device, and enforces a global spend kill-switch.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.0"
  }
}
